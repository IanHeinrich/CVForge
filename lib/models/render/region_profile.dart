/// Region-specific document conventions (page format, date/spelling
/// normalisation). One value in Phase 1 — this exists purely as the seam a
/// future US preset (Letter format, American spelling) plugs into without
/// re-signing [CvComposer.compose] or every call site.
enum RegionProfile { uk }
