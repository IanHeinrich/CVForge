/// The max width the content can ever take up on the screen
const double kdDesktopMaxContentWidth = 1150;

/// The max height a desktop view's content will take up
const double kdDesktopMaxContentHeight = 750;

/// Container/edge padding scale for card and panel chrome. Deliberately
/// separate from ui_helpers.dart's tiny/small/medium/large scale, which
/// spaces sibling widgets apart rather than padding the inside of a
/// container's border.
const double kdPaddingTight = 8;
const double kdPaddingCompact = 12;
const double kdPaddingDefault = 16;
const double kdPaddingPanel = 20;
const double kdPaddingPage = 24;
