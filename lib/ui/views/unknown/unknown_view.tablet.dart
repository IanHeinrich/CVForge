import 'unknown_view.desktop.dart';

/// Identical to desktop — a 404 has nothing to adapt per breakpoint. See
/// [UnknownViewDesktop] for the shared implementation.
class UnknownViewTablet extends UnknownViewDesktop {
  const UnknownViewTablet({super.key});
}
